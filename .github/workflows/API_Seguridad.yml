name: Deploy Seguridad

env:
  AZURE_WEBAPP_NAME: APISeguridadJorge    # Cambia esto por el nombre de tu Azure Web App
  AZURE_WEBAPP_PACKAGE_PATH: './Seguridad/Seguridad.API/API'     # Ruta al proyecto API
  DOTNET_VERSION: '8.0'                  # Versión .NET 8.0 según tu proyecto

on:
  push:
    branches: [ "main" ]
    paths:
      - 'Seguridad/Seguridad.API/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: 'Production-Database'

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Configurar Nuget feed privado
        run: |
          dotnet nuget add source \
            "https://nuget.pkg.github.com/SC-701/index.json" \
            --name "Paquetes" \
            --username "${{ github.actor }}" \
            --password "${{ secrets.PAQUETESTOKEN }}" \
            --store-password-in-clear-text

      - name: Restore dependencies
        run: |
          cd "Seguridad/Seguridad.API"
          dotnet restore "Abstracciones/Abstracciones.csproj"
          dotnet restore "DA/DA.csproj"
          dotnet restore "Flujo/Flujo.csproj"
          dotnet restore "Reglas/Reglas.csproj"
          dotnet restore "Helpers/Helpers.csproj"
          dotnet restore "API/API.csproj"
          
      - name: Compilar
        run: |
          cd "Seguridad/Seguridad.API"
          dotnet build "API/API.csproj" --configuration Release --no-restore

      - name: Publicar (dotnet publish)
        run: |
          cd "Seguridad/Seguridad.API"
          dotnet publish "API/API.csproj" \
            --configuration Release \
            --no-build \
            --output ./publish

      - name: Deploy a Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name:        ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_SEGURIDAD }}
          package:         './Seguridad/Seguridad.API/publish'